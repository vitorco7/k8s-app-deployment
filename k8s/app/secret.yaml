# This file is intentionally empty.
# The Secret is generated by Kustomize's secretGenerator in kustomization.yaml,
# reading from k8s/app/secret.env (git-ignored).
# See k8s/app/secret.env.example for the required keys.
